export const PIXELS_PER_INCH = 11
export const SQUARE_INCHES = 1
export const SQUARE_SIZE = PIXELS_PER_INCH * SQUARE_INCHES
// extra room on the right/bottom so the last grid line (drawn by the
// background pattern) isn't clipped exactly at the edge by overflow-hidden
export const GRID_EDGE_PADDING = 1
